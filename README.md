# Sniphub 🧬

Проект, посвященный разработке бота для анализа однонуклеотидных вариантов на базе данных dbSNP NCBI.

## Введение 

`Sniphub Bot` — это Telegram бот, позволяющий получать и анализировать данные об однонуклеотидных полиморфизмах из базы данных NCBI dbSNP. 

Бот извлекает частоты аллелей, рассчитывает частоты генотипов по закону Харди-Вайнберга и визуализирует результаты в виде графиков.

## Основные задачи

-  **Поиск SNP** по rsID
-  **Анализ частот аллелей** из различных исследований
-  **Расчет частот генотипов** по закону Харди-Вайнберга
-  **Визуализация результатов**:
  - Столбчатые диаграммы частот аллелей
  - Круговые диаграммы распределения генотипов
-  **Структурированные данные** в формате JSON
-  **Кэширование результатов** с использованием Redis
-  **История запросов** пользователя
-  **Ограничение запросов** для предотвращения злоупотреблений
 
## Предварительные требования

- **Токен Telegram бота** от [@BotFather](https://t.me/BotFather)

## Установка

1. Клонируйте репозиторий:
   ```bash
   git clone <repository-url>
   cd sniphub
   ```
2. Создайте файл конфигурации:
   ```bash
   cp .env.example .env
   ```
3. Настройте конфигурацию в файле .env:
   ```
   TELEGRAM_BOT_TOKEN=токен_бота
   REDIS_URL=redis://localhost:6379/0 NCBI_API_BASE=https://api.ncbi.nlm.nih.gov/variation/v0/refsnp
   NCBI_API_TIMEOUT=30
   CACHE_TTL=86400
   MAX_REQUESTS_PER_HOUR=50
   EXAMPLE_RSIDS=rs7755898,rs429358,rs7412
   ```
4. Соберите и запустите Docker контейнер:
   ```bash
   docker-compose up --build -d
   ```
5. Проверьте работу бота:
   ```bash
   docker-compose logs -f bot
   ```
6. Найдите бота в Telegram и отправьте команду `/start`

### Установка без Docker

**Требования:** Python 3.9+, Redis

1. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
2. Установите и запустите Redis:
   ```bash

   sudo apt-get install redis-server
   sudo systemctl start redis
   
   redis-cli ping
   ```
3. Настройте переменные окружения (как в шаге 3 выше)
4. Запустите бота:
   ```
   bash
   python snp_bot.py
   ```
---

**Структура проекта**

```
Sniphub
├── Dockerfile                    #  Docker
├── .gitignore
├── src/
    ├── snp_bot.py                # Основной файл бота
    ├── requirements.txt          # Зависимости Python
    ├── .env                      # Файл конфигурации
├── ouput/
    ├── rs10
    └── rs7412                    # Примеры запросов
└── README.md                     # Документация
```

🤖 **Команды бота**
```
/start - начало работы и приветствие
/help- справка по командам
/get <rsid> - получить данные по rsID
/history - история запросов
/about - информация о боте
```

--- 
**Пример использования**

1. Отправьте боту команду `/start`
2. Нажмите на одну из кнопок с примерами `rsID` или используйте команду `/get` rs7755898
3. В результате бот выдаст информацию: текстовый отчет с анализом, визуализация и JSON файл с полными данными

---
 **Поддержка**

Если у вас возникли вопросы или проблемы: создайте issue в репозитории

---
```
Ссылка на бота: @snip_analyzer_bot
```
